# Repository Agent Guidance

This project was built with the microsoft-foundry skill. Before working on or answering questions about Foundry agents, read the microsoft-foundry skill first.

Preserve the pinned `azure-ai-projects==2.4.0` contract. The multi-agent lab uses `agent-framework-core`, `agent-framework-openai`, and `agent-framework-orchestrations` directly because the independently versioned `agent-framework-foundry` adapter is not compatible with that projects SDK release.