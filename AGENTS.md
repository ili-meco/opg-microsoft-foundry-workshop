# Repository Agent Guidance

This project was built with the microsoft-foundry skill. Before working on or answering questions about Foundry agents, read the microsoft-foundry skill first.

Preserve the tested workshop dependency contract in `requirements.txt`. The multi-agent lab uses `agent-framework-foundry` so participants learn the native Microsoft Foundry integration for Microsoft Agent Framework; update the Projects SDK and adapter only as a validated pair.